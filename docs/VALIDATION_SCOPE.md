# npuloop 검증 범위

이 문서는 정수 실행의 측정값과 가상 NPU 비용 추정을 구분하기 위한 기준이다. 실제 모빌린트 하드웨어 또는 고객 데이터에서 실행했다는 뜻이 아니다.

현재 모델 실험 화면은 기존 E1–E20 기록을 비교하고, [통합 실행기](STUDY_RUNNER.md)의 새 결과를 별도로 가져옵니다. Python 실행기는 실제 구조 변경·학습·보정·정수 평가를 수행합니다. 가져온 결과는 동일 시험 부분집합, 정확한 이미지 수, 측정 출처와 학습 로그를 검사해 표시합니다. 파일의 내용 자체를 제삼자가 인증한 것은 아닙니다.

| 구분 | 근거가 있는 범위 | 확대해서 말하지 않을 범위 |
|---|---|---|
| NumPy ↔ C++ | 테스트에 포함된 모델·입력의 정수 코드 및 중간 텐서 일치 | 모든 입력의 정확성, 두 구현의 공통 오류 부재 |
| 파일 export ↔ 독립 실행형 C++ | 직렬화·역직렬화 후 시험 입력의 결과 일치 | 제삼의 독립 알고리즘 검증, 실제 NPU 실행 |
| E11 외부 대조 | 지정 TFLite reference 경로, conv(stride 1·2)·MEAN·fully-connected를 포함한 시험 모델, 1,000장 비교 | depthwise·add·softmax·LayerNorm 등 전체 연산자의 TFLite 일치 |
| E8 비용 대조 | SCALE-Sim과 공통 가정 아래 dense conv·linear 연산 사이클 비교 | DRAM 스톨·벡터 엔진·멀티코어·실제 실리콘 성능 |
| E13 Vela 대조 | 두 해석적 추정기의 차이(약 0.29–0.84배) | Vela도 실측이라는 주장, 특정 비용 항이 원인이라는 인과 확정 |
| E15 정확도·출력 코드 | 분류 모델 6종 × 방식 2종, 전체 테스트셋의 기존 저장 결과 | ±0.20%p 이내의 통계적 동등성, 모든 모델로의 일반화 |
| E15·E17 연산자별 국소 차이 | 고정 표본 배치의 교사 강제 측정. 종류 평균은 노드별 비율의 단순평균 | 전체 테스트셋의 모든 노드 조사, 텐서 크기 가중 평균 |
| E16 개입 | LayerNorm만 정수 연산으로 에뮬레이션했을 때의 결과 | 모든 연산자의 누적 교체가 불가능하다는 결론 |
| E17 밀집 출력 | 3층 ESPCN ×2, Imagenette 기반 초해상 조건 | 깊이가 원인이라는 확정, 더 깊은 모델의 불일치 하한 |

## 결과별로: 무엇이 답해지고 무엇이 답해지지 않는가

세 번째 열이 이 저장소의 성격이다. 표는 `results/*.json` 에서 `make tables` 로 생성된다.

<!-- TABLE:HEADLINE -->
| 질문 | `results/*.json`이 답하는 것 | 단, 검증되지 않은 것 |
|---|---|---|
| **정수 엔진을 믿어도 되는가** ([E11](docs/EXPERIMENTS.md#e11)) | TFLite reference 커널과 **1,000장 × 모든 텐서 0 불일치**(conv·pool은 gemmlowp 이중 반올림, fc는 단일 반올림) | 대조 범위는 conv(stride 1·2)·MEAN·fully-connected. depthwise·add·softmax는 미대조이고, TFLite 자신도 XNNPACK 경로와 reference가 155/1,000장 다릅니다 |
| **비용 모델이 맞는가** ([E8](docs/EXPERIMENTS.md#e8)) | SCALE-Sim v3와 12개 (모델, 배열) 조합에서 합계 오차 ≤ 0.08%, 최악 레이어 0.5% | 검증된 것은 dense conv·linear의 연산 사이클(단일 코어, 메모리 스톨 없음). depthwise·벡터 패스·멀티코어·DRAM roofline은 검증되지 않았습니다 |
| **그럼 실리콘에 가까운가** ([E13](docs/EXPERIMENTS.md#e13)) | Arm Vela 대비 **5/5 낙관적**(사이클 비 0.29~0.84) | **아니오.** 둘 다 해석적 추정기이고 Vela는 컴파일된 스케줄(fusion·타일링)을, 이쪽은 레이어를 하나씩 셉니다. E8의 일치는 하드웨어 충실도가 아니라 같은 이상화를 공유하는 두 모델이 같은 식을 같게 구현했다는 확인입니다 |
| **fake-quant 정확도를 믿어도 되는가** ([E2](docs/EXPERIMENTS.md#e2)·[E15](docs/EXPERIMENTS.md#e15)) | 95% Wald 구간의 0 포함 12/12; 최대 관측 정확도 차이 0.20%p (정수 − 모의, 전체 테스트셋). 허용 오차 내 동등성 입증은 아님 | E15·E16의 16개 비교를 다중비교 보정하지 않은 값입니다(E16까지 합치면 최대 \|Δ\|/SE 2.2). 모델 5개 + Imagenette 1개, 스킴 2개 범위 |
| **텐서도 같은가** ([E15](docs/EXPERIMENTS.md#e15)·[E16](docs/EXPERIMENTS.md#e16)) | 그러나 출력 코드는 28.5~72.7%가 다릅니다. 가장 큰 국소 원천(ViT의 LayerNorm)을 정수 산술로 바꿔 국소 불일치를 24.77% → 0.00%로 없애도 출력 코드 불일치는 72.7% → 65.7%까지만 내려갑니다 | 출력 코드 불일치는 test 전체, 국소 불일치는 125/250장 배치의 teacher-forced 값입니다 — 두 수를 같은 문장에서 섞지 마세요 |
| **싸구려 반올림의 값** ([E14](docs/EXPERIMENTS.md#e14)) | requant에서 반올림 대신 시프트를 쓰면 18개 (모델, 시드, 모드) 조합 **18개 전부**에서 손해입니다(-3.37~-0.90%p, \|Δ\|/SE 4.4~11.8) | 강건한 것은 부호와 유의성이고 **크기는 아닙니다**: SiLU의 truncate는 시드에 따라 −1.36~−2.98%p(시드 SD가 효과의 37%). 한 시드 숫자를 그 모드의 비용으로 인용하면 안 됩니다 |
| **그 밖의 정수 구현 세부** ([E7](docs/EXPERIMENTS.md#e7)) | 13개 구현 구성 중 7개는 무손실, 2개는 모델을 무너뜨립니다 (곱셈기 7비트·누산기 20비트까지는 공짜, 바이어스 12비트·누산기 16비트는 붕괴) | 바이어스 폭 축은 지수 조정 없는 **클리핑**을 재고(시판 NPU는 더 넓습니다 — Vela는 40비트), 누산기 포화는 부분합이 아니라 최종합에 한 번만 걸리며, 결과는 K ≤ 1,280인 이 모델들에 한정됩니다 |
<!-- /TABLE:HEADLINE -->

## 수치의 읽는 법

분류의 **top-1 정확도**, 두 실행의 **top-1 예측 일치율**, **출력 코드 불일치율**은 다른 지표다. 출력 코드가 다르다고 동일 비율의 분류가 틀린 것은 아니다. 국소 불일치율은 한 연산자의 입력을 맞춰 비교한 값이며 전파 불일치율과 가산 분해 관계가 아니다.

E15의 관측 정확도 차이 최대 0.20%p와 신뢰구간의 0 포함은 동등성 입증이 아니다. E16의 pc 교체 조건은 약 2.2 표준오차다. 같은 정수 실행을 기준으로 한 두 차이만으로 개입에 따른 정확도 예측 개선을 검정하지 않는다.

표 3.1에서 LayerNorm 이외 **종류 평균**은 1% 미만이지만, 풀링 **개별 노드**는 최대 2.697%다. E14의 18개 비교는 **3개 모델 × 3개 학습 반복 × 2개 반올림 모드(truncate/floor)**이며, 두 양자화 방식의 조합을 뜻하지 않는다.

## 2026-09-16 초기 로컬 통합 단계의 실행 검증 기록

아래 수치는 초기 통합 단계의 기록입니다. 이후 공개 Workbench 게시 단계의 기록은 `verification/publish/`와 [배포 안내](DEPLOYMENT.md)에 있으며, 현재 공개 상태는 [GitHub Actions](https://github.com/sokldjs554/npuloop/actions)에서 확인합니다. 아래의 147개 테스트 수를 최신 전체 테스트 수로 사용하지 않습니다.

업로드한 전체 소스에 수정 사항을 적용한 **로컬 Linux 통합본**을 실행했다. 기본 전체 테스트는 147개 통과·4개 건너뜀이고, `make verify`에서 번들 체크포인트의 quickstart와 결과 JSON 기반 논문 표 재생성 검사도 완료했다. 두 학습된 번들 모델은 각각 시험 이미지 8장으로 NumPy·C++·파일 export·독립 C++ 실행기의 중간 텐서를 별도 대조했다. 상세 수와 로그는 [통합 검증 기록](INTEGRATION_REPORT.md)에 있다.

데모는 Chromium에서 오프라인 HTML로 로드하여 146개 컨트롤 조건과 20개 비용 프리셋·10개 인테이크 결과를 확인했다. 외부 글꼴 CSS는 비워 로컬 시스템 글꼴로 표시했다. 공개 GitHub Pages 접속이나 원격 배포를 검증한 것은 아니다.

이 결과를 모든 실험의 독립 재실행으로 합쳐 말하지 않는다. 전체 CIFAR-10·Imagenette 데이터와 전체 원본 학습 체크포인트는 업로드에 없었다. TFLite·SCALE-Sim·Vela 실험의 기존 JSON을 다시 읽고 표를 만들었지만 외부 도구 실험 자체를 새로 실행하지 않았다. 실제 NPU 하드웨어·모빌린트 SDK·Windows 네이티브 실행도 이번 검증 대상이 아니다.

근거: `paper/npuloop_thesis.tex`의 표 3.1, 5.2–5.8 및 6.1절; `experiments/e15_fidelity.py`; `experiments/common.py`; `verification/verify_final.log`; `verification/bundled_engines.json`; `verification/browser/report.json`.

## 모델 변경·경량화 결과의 비교 조건

| 실험 | 정확도 측정 조건 | 비용·해석 범위 |
|---|---|---|
| E4 활성함수 변경·QAT | FP32·모의 양자화·정수 실행을 구분하며, 저장 결과의 정수 평가 표본 수는 10,000장 | 재학습은 구조 변경 후 정확도 회복을 측정한 것. QAT가 모든 모델에서 개선된 것은 아님 |
| E6 프루닝 | `ft_acc`는 3 epoch 미세조정 후 FP32, `int8_acc`는 모의 양자화 평가 | `ratio`는 전략마다 채널 유지 비율 또는 목표 사이클 비율로 의미가 다름. 배열 정렬의 일률적 우월성이나 실측 NPU 가속을 주장하지 않음 |
| E9 ViT 변경 | FP32·전체 모의 정확도는 전체 테스트셋, `int_acc`와 `fake_acc_on_int_subset`은 고정된 2,000장 부분집합 | 같은 열의 변경 전후를 비교. 서로 다른 표본 수의 FP32−INT8 차이를 양자화 손실로 계산하지 않음 |

정확도와 사이클은 각각 측정·추정으로 구분합니다. E12는 데이터셋과 입력 크기가 함께 달라지므로 결과 차이를 입력 크기 하나의 효과로 해석하지 않습니다. 모델 변경 전후의 반복 학습 결과를 새로 확보하는 작업은 기존 E14 반올림 반복 실험과 별개입니다.
